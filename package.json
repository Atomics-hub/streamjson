{
  "name": "streamjson-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "node --test test/**/*.test.js",
    "bench": "node test/bench/streaming-benchmark.js"
  },
  "devDependencies": {
    "partial-json": "^0.1.7",
    "best-effort-json-parser": "^1.4.0",
    "jsonrepair": "^3.13.3"
  }
}
